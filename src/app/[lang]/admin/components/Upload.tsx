import React from "react";
import SignedUpload from "../../upload/components/SignUpload";

const Upload = () => {
  return (
    <div className="p-[2rem] w-full">
      <SignedUpload />
    </div>
  );
};

export default Upload;
